# PDF Export Requirements for Resume Builder
# These are optional dependencies for full PDF export functionality
# Install with: pip install -r requirements-pdf.txt

# Core PDF generation (pure Python - no system dependencies required)
xhtml2pdf>=0.2.17
reportlab>=4.0.0

# Note: xhtml2pdf is a pure Python solution that doesn't require
# system libraries like Pango or Cairo, making it easier to deploy.

# Alternative PDF libraries (if xhtml2pdf doesn't work)
# weasyprint>=60.0  # Requires system libraries: pango, cairo
# reportlab>=4.0.0  # Lower-level PDF generation